# Vite exposes these values in the browser bundle; use public URLs only.
VITE_API_URL=http://localhost:3001
# Leave empty to derive ws:// or wss:// from VITE_API_URL.
VITE_WS_URL=
VITE_APP_NAME=LifePlatform
